module.exports = {
  // endpoint: 'https://self-hosted.gitlab/api/v4/',
  // token: '**gitlab_token**',
  platform: 'github',
  onboardingConfig: {
    extends: ['config:recommended'],
  },
  repositories: ['omad/nixos'],
};
