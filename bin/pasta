#!/usr/bin/env bash
#
# Thanks https://evanhahn.com/scripts-i-wrote-that-i-use-all-the-time/
set -e
set -u
set -o pipefail

if hash pbpaste 2>/dev/null; then
	exec pbpaste
elif hash xclip 2>/dev/null; then
	exec xclip -selection clipboard -o
else
	echo 'cannot find a paste program' >&2
	exit 1
fi
